---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const validateDate = new Date(date);

---
<time datetime={validateDate.toISOString()}>
	{
		validateDate.toLocaleDateString('en-us', {
			year: 'numeric',
			month: 'short',
			day: 'numeric',
		})
	}
</time>
